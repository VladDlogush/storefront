import React from 'react';

const Loader = () => {
  const Style = {
    fontFamily: 'Roboto',
    fontWeight: 700,
    textAlign: 'center',
    color: '#1d1d20',
  };

  return <h1 style={Style}>Loading...</h1>;
};

export default Loader;
