import React from 'react';

const NotFoundPage = () => {
  const Style = {
    fontFamily: 'Roboto',
    fontWeight: 700,
    textAlign: 'center',
    color: '#1d1d20',
  };

  return <h1 style={Style}>Sorry, page not found</h1>;
};

export default NotFoundPage;
